<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dvlopapps.shporgalki"
    android:versionCode="16"
    android:versionName="1.3">

    <permission android:name="INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/icon"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Design.NoActionBar">
        <activity android:name=".KipenieplavlenieActivity"></activity>
        <activity android:name=".Physics" />
        <activity android:name=".FunnyActivity" />
        <activity android:name=".InformatikaActivity" />
        <activity android:name=".FactorialActivity" />
        <activity android:name=".ChimActivity" />
        <activity android:name=".StepeniActivity" />
        <activity android:name=".MathActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>