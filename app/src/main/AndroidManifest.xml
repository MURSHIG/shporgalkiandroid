<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.murat.shporgalki">

    <application
        android:allowBackup="true"
        android:icon="@drawable/icon"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Shporgalki">
        <activity android:name=".InformatikaActivity"></activity>
        <activity android:name=".FactorialActivity" />
        <activity android:name=".ChimActivity" />
        <activity android:name=".StepeniActivity" />
        <activity android:name=".Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MathActivity" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>